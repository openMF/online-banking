export const environment = {
  production: true,
  fineractPlatformTenantId: 'default',  // For connecting to server running elsewhere update the tenant identifier
  baseApiUrl: 'https://demo.mifos.community',  // For connecting to server running elsewhere update the base API URL
  apiProvider: '/fineract-provider/api',
  apiVersion: '/v1',
  serverUrl: 'https://demo.mifos.community/fineract-provider/api/v1',
  selfServiceRoleId: 2,
};
